export type Role = { id: number; name: string };
export type Status = { id: number; name: string };
export type UserRow = { id: number; name: string; email: string; role_id: number; role: string, status_id: number; status: string };
